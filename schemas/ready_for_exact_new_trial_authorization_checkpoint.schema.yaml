id: ready_for_exact_new_trial_authorization_checkpoint
version: v0_1
phase: v0_6_5_ready_for_exact_new_trial_authorization_checkpoint
type: object
required:
  - checkpoint_id
  - version
  - phase
  - source_failed_provider_attempt_inspection_ref
  - source_exact_new_trial_authorization_refresh_ref
  - checkpoint_assertions
  - authorization_readiness
  - next_route_options
  - boundaries
  - side_effects
properties:
  checkpoint_assertions:
    required:
      - failed_provider_attempt_inspection_exists
      - exact_new_trial_authorization_refresh_exists
      - ready_for_exact_new_trial_authorization
      - future_exact_approval_phrase_required
      - can_execute_now
  authorization_readiness:
    required:
      - first_attempt_failed_no_image
      - retry_001_failed_no_image
      - smoke_001_succeeded_image_generated
      - safe_portrait_001_succeeded_image_generated
      - reuse_original_prompt_by_default
      - reuse_retry_001_prompt_by_default
      - explicit_new_prompt_package_or_override_required
      - new_output_directory_required
      - new_receipt_path_required
      - new_registry_path_required
      - new_review_console_bridge_ref_required
      - max_provider_calls_still_1
      - max_image_candidates_still_1
      - retry_limit_still_0
      - no_memory_write_default_still_true
  boundaries:
    required:
      - metadata_only
      - checkpoint_only
      - authorization_ready_only
      - can_execute_now
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
