id: exact_new_trial_intake_field_resolution
version: v0_1
phase: v0_6_8_exact_new_trial_intake_field_resolution
type: object
required:
  - authorization_package_id
  - version
  - phase
  - source_intake_preflight_ref
  - source_exact_new_trial_a5_request_draft_ref
  - source_ready_for_exact_new_trial_authorization_checkpoint_ref
  - source_safe_portrait_receipt_ref
  - source_safe_portrait_registry_ref
  - resolution_status
  - resolved_exact_fields
  - cross_field_consistency
  - fixed_constraints_confirmation
  - pre_submission_assertions
  - next_route_options
  - boundaries
  - side_effects
properties:
  resolution_status:
    required:
      - authorization_status
      - approval_status
      - active
      - execute_now
      - submit_ready
      - request_text_regenerated_after_resolution
      - resolution_only
  resolved_exact_fields:
    required:
      - prompt_package_ref_or_override
      - output_directory
      - receipt_path
      - registry_path
      - review_console_bridge_ref
  cross_field_consistency:
    required:
      - shared_trial_key
      - output_receipt_registry_bridge_share_same_key
      - every_selected_value_is_new_not_reused_from_failed_attempts
      - review_console_bridge_root_corrected_from_preflight_placeholder_note
      - safe_portrait_prompt_selected_as_closest_successful_person_portrait_route
  fixed_constraints_confirmation:
    required:
      - provider_target
      - plugin_id_or_provider_route
      - model
      - command
      - exact_call_count
      - max_image_candidates
      - retry_limit
      - review_required_after_generation
      - no_memory_write_default
      - overwrite_existing_files_allowed
  pre_submission_assertions:
    required:
      - all_five_exact_fields_resolved_locally
      - request_text_regenerated_after_resolution
      - ready_to_regenerate_request_text
      - exact_human_A5_phrase_still_required
      - can_submit_now
      - can_execute_now
  boundaries:
    required:
      - metadata_only
      - resolution_only
      - request_not_submitted
      - can_submit_now
      - can_execute_now
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
