id: first_controlled_generation_authorization_packet
version: v0_1
phase: v0_6_0_first_controlled_generation_authorization_packet
type: object
required:
  - authorization_packet_id
  - version
  - phase
  - source_readiness_packet_ref
  - source_human_review_gate_packet_ref
  - source_evidence_contract_ref
  - source_visual_memory_readonly_query_contract_ref
  - explicit_A5_required
  - exact_call_count
  - allowed_output_dir_policy
  - review_required_after_generation
  - no_memory_write_default
  - boundaries
  - side_effects
properties:
  explicit_A5_required:
    required:
      - enabled
      - separate_human_authorization_required
      - execution_authorized_now
  exact_call_count:
    required:
      - configured_limit
      - actual_generation_calls
      - provider_call_budget_consumed
  allowed_output_dir_policy:
    required:
      - output_dir_preapproved
      - output_directory_selection_required_before_execution
      - raw_local_path_allowed
      - output_write_performed
      - project_relative_output_dir_required
  review_required_after_generation:
    required:
      - enabled
      - human_review_required_after_generation
      - accepted_sample_promotion_allowed
      - production_candidate_allowed
  no_memory_write_default:
    required:
      - enabled
      - VCP_memory_write_allowed
      - DailyNote_write_allowed
      - memory_seed_promotion_allowed
  boundaries:
    required:
      - metadata_only
      - authorization_packet_only
      - preflight_only
      - execution_authorized_by_this_packet
      - provider_call_performed
      - image_generation_performed
      - output_write_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - provider_call_performed
      - image_generation_performed
      - output_write_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
