title: Exact New-Trial No-op Rehearsal And Human Approval Gate
type: object
required:
  - exact_new_trial_noop_rehearsal_human_approval_gate
properties:
  exact_new_trial_noop_rehearsal_human_approval_gate:
    type: object
    required:
      - authorization_package_id
      - action_packet_id
      - noop_rehearsal_id
      - human_approval_packet_id
      - version
      - phase
      - source_action_packet_ref
      - noop_execution_report
      - human_approval_packet
      - exact_future_approval_statement_template
      - stop_conditions
      - boundaries
      - side_effects
    properties:
      authorization_package_id:
        const: AUTH-PENDING-V0-3-3-EXACT-NEW-TRIAL-20260523-001
      action_packet_id:
        const: exact_new_trial_action_packet_v0_1
      noop_rehearsal_id:
        const: exact_new_trial_noop_rehearsal_v0_1
      human_approval_packet_id:
        const: exact_new_trial_human_approval_packet_v0_1
      version:
        const: v0_1
      phase:
        const: v0_6_15_exact_new_trial_noop_rehearsal_human_approval_gate
      source_action_packet_ref:
        type: string
      noop_execution_report:
        type: object
      human_approval_packet:
        type: object
      exact_future_approval_statement_template:
        type: string
      stop_conditions:
        type: array
      boundaries:
        type: object
      side_effects:
        type: object
