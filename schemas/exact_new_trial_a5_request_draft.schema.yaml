id: exact_new_trial_a5_request_draft
version: v0_1
phase: v0_6_6_exact_new_trial_a5_request_draft
type: object
required:
  - authorization_package_id
  - version
  - phase
  - source_exact_new_trial_authorization_refresh_ref
  - source_ready_for_exact_new_trial_authorization_checkpoint_ref
  - draft_status
  - fixed_constraints
  - unresolved_required_fields
  - exact_approval_phrase_draft
  - next_route_options
  - boundaries
  - side_effects
properties:
  draft_status:
    required:
      - authorization_status
      - approval_status
      - active
      - execute_now
      - exact_approval_phrase_received
  fixed_constraints:
    required:
      - provider_target
      - plugin_id_or_provider_route
      - model
      - command
      - max_provider_calls
      - max_image_candidates
      - retry_limit
      - no_memory_write_default
      - overwrite_existing_files_allowed
  unresolved_required_fields:
    required:
      - exact_new_prompt_package_or_override_statement_required
      - exact_new_output_directory_required
      - exact_new_receipt_path_required
      - exact_new_registry_path_required
      - exact_new_review_console_bridge_ref_required
  exact_approval_phrase_draft:
    required:
      - recommended_exact_approval_phrase_present
      - draft_uses_placeholders_only
      - draft_not_executable_until_placeholders_replaced
      - approval_phrase_draft
  boundaries:
    required:
      - metadata_only
      - draft_only
      - request_not_submitted
      - can_execute_now
      - ready_for_exact_new_trial_authorization
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
