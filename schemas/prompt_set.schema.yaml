# prompt_set.schema.yaml
# 提示词集合 schema：一组 prompt_package 的引用集合
prompt_set:
  prompt_set_id: string
  version: integer
  created_at: string
  description: string

  packages:
    - prompt_package_id: string
      priority: integer | null
      tags:
        - string
