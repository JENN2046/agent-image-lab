id: visual_memory_readonly_query_contract
version: v0_1
phase: v0_5_9_visual_memory_readonly_query_contract
type: object
required:
  - visual_memory_readonly_query_contract_id
  - version
  - phase
  - source_visual_memory_readonly_plan_ref
  - source_visual_sample_memory_policy_ref
  - source_accepted_sample_schema_ref
  - source_rejected_sample_schema_ref
  - accepted_sample_readonly_query
  - rejected_pattern_readonly_query
  - style_dna_readonly_query
  - no_write_flags
  - boundaries
  - side_effects
properties:
  accepted_sample_readonly_query:
    required:
      - enabled
      - read_only
      - query_shape_only
      - returns_metadata_only
      - execution_allowed_now
      - real_memory_read_performed
      - accepted_sample_created
      - accepted_sample_auto_promotion
  rejected_pattern_readonly_query:
    required:
      - enabled
      - read_only
      - query_shape_only
      - returns_metadata_only
      - execution_allowed_now
      - real_memory_read_performed
      - rejected_sample_created
      - failure_lesson_written
  style_dna_readonly_query:
    required:
      - enabled
      - read_only
      - query_shape_only
      - returns_metadata_only
      - execution_allowed_now
      - real_memory_read_performed
      - memory_seed_promoted
  no_write_flags:
    required:
      - enabled
      - VCP_memory_write_allowed
      - DailyNote_write_allowed
      - accepted_sample_promotion_allowed
      - memory_seed_promotion_allowed
      - production_candidate_write_allowed
  boundaries:
    required:
      - metadata_only
      - readonly_query_contract_only
      - query_contract_dry_run_only
      - no_live_memory_access
      - real_memory_read_performed
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - real_memory_read_performed
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
