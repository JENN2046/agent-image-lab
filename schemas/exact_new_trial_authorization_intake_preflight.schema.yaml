id: exact_new_trial_authorization_intake_preflight
version: v0_1
phase: v0_6_7_exact_new_trial_authorization_intake_preflight
type: object
required:
  - authorization_package_id
  - version
  - phase
  - source_exact_new_trial_a5_request_draft_ref
  - source_ready_for_exact_new_trial_authorization_checkpoint_ref
  - source_first_controlled_generation_authorization_packet_ref
  - intake_status
  - required_exact_fields_checklist
  - fixed_constraints_confirmation
  - preflight_assertions
  - next_route_options
  - boundaries
  - side_effects
properties:
  intake_status:
    required:
      - authorization_status
      - approval_status
      - active
      - execute_now
      - submit_ready
      - preflight_only
  required_exact_fields_checklist:
    required:
      - prompt_package_ref_or_override
      - output_directory
      - receipt_path
      - registry_path
      - review_console_bridge_ref
  fixed_constraints_confirmation:
    required:
      - provider_target
      - plugin_id_or_provider_route
      - model
      - command
      - exact_call_count
      - max_image_candidates
      - retry_limit
      - review_required_after_generation
      - no_memory_write_default
      - overwrite_existing_files_allowed
  preflight_assertions:
    required:
      - placeholders_grouped_into_one_checklist
      - all_placeholders_still_unresolved_at_this_phase
      - exact_human_A5_phrase_still_required
      - regenerate_request_text_after_resolution
      - can_submit_now
      - can_execute_now
  boundaries:
    required:
      - metadata_only
      - intake_only
      - preflight_only
      - request_not_submitted
      - can_submit_now
      - can_execute_now
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - real_executor_implemented_now
      - Push_L2_exercised
      - package_dependency_change_performed
      - commit_performed
      - push_performed
  side_effects:
    required:
      - provider_call_performed
      - image_generation_performed
      - VCP_memory_write_performed
      - DailyNote_write_performed
      - runtime_call_performed
      - secret_value_read_performed
      - production_candidate_created
      - accepted_sample_auto_promotion
      - memory_seed_promoted
      - package_dependency_change_performed
      - commit_performed
      - push_performed
